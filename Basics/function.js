// Defined : Function ek block of code hai jo baar-baar reuse kiya ja sakta hai.

// Why use?
// Code reusability
// Modularity
// Maintainability

// Normal Function
function add(a, b) {
  return a + b;
}

// Arrow Function
const multiply = (x, y) => x * y;

console.log(add(2, 3));
console.log(multiply(4, 5));
